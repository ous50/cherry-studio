diff --git a/dist/index.d.mts b/dist/index.d.mts
index 27adfbe90096f466c681e9a6c5b2f24109054f20..83c7c0b8e201a15de01c032f2af5da964b53e0e5 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -11,7 +11,7 @@ declare const googleErrorDataSchema: _ai_sdk_provider_utils.LazySchema<{
 }>;
 type GoogleErrorData = InferValidator<typeof googleErrorDataSchema>;
 
-type GoogleGenerativeAIModelId = 'gemini-1.5-flash' | 'gemini-1.5-flash-latest' | 'gemini-1.5-flash-001' | 'gemini-1.5-flash-002' | 'gemini-1.5-flash-8b' | 'gemini-1.5-flash-8b-latest' | 'gemini-1.5-flash-8b-001' | 'gemini-1.5-pro' | 'gemini-1.5-pro-latest' | 'gemini-1.5-pro-001' | 'gemini-1.5-pro-002' | 'gemini-2.0-flash' | 'gemini-2.0-flash-001' | 'gemini-2.0-flash-live-001' | 'gemini-2.0-flash-lite' | 'gemini-2.0-pro-exp-02-05' | 'gemini-2.0-flash-thinking-exp-01-21' | 'gemini-2.0-flash-exp' | 'gemini-2.5-pro' | 'gemini-2.5-flash' | 'gemini-2.5-flash-image-preview' | 'gemini-2.5-flash-lite' | 'gemini-2.5-flash-lite-preview-09-2025' | 'gemini-2.5-flash-preview-04-17' | 'gemini-2.5-flash-preview-09-2025' | 'gemini-3-pro-preview' | 'gemini-pro-latest' | 'gemini-flash-latest' | 'gemini-flash-lite-latest' | 'gemini-2.5-pro-exp-03-25' | 'gemini-exp-1206' | 'gemma-3-12b-it' | 'gemma-3-27b-it' | (string & {});
+type GoogleGenerativeAIModelId = 'gemini-2.0-flash' | 'gemini-2.0-flash-001' | 'gemini-2.0-flash-live-001' | 'gemini-2.0-flash-lite' | 'gemini-2.0-pro-exp-02-05' | 'gemini-2.0-flash-thinking-exp-01-21' | 'gemini-2.0-flash-exp' | 'gemini-2.5-pro' | 'gemini-2.5-flash' | 'gemini-2.5-flash-image-preview' | 'gemini-2.5-flash-lite' | 'gemini-2.5-flash-lite-preview-09-2025' | 'gemini-2.5-flash-preview-04-17' | 'gemini-2.5-flash-preview-09-2025' | 'gemini-3-pro-preview' | 'gemini-pro-latest' | 'gemini-flash-latest' | 'gemini-flash-lite-latest' | 'gemini-2.5-pro-exp-03-25' | 'gemini-exp-1206' | 'gemma-3-12b-it' | 'gemma-3-27b-it' | (string & {});
 declare const googleGenerativeAIProviderOptions: _ai_sdk_provider_utils.LazySchema<{
     responseModalities?: ("TEXT" | "IMAGE")[] | undefined;
     thinkingConfig?: {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 27adfbe90096f466c681e9a6c5b2f24109054f20..83c7c0b8e201a15de01c032f2af5da964b53e0e5 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -11,7 +11,7 @@ declare const googleErrorDataSchema: _ai_sdk_provider_utils.LazySchema<{
 }>;
 type GoogleErrorData = InferValidator<typeof googleErrorDataSchema>;
 
-type GoogleGenerativeAIModelId = 'gemini-1.5-flash' | 'gemini-1.5-flash-latest' | 'gemini-1.5-flash-001' | 'gemini-1.5-flash-002' | 'gemini-1.5-flash-8b' | 'gemini-1.5-flash-8b-latest' | 'gemini-1.5-flash-8b-001' | 'gemini-1.5-pro' | 'gemini-1.5-pro-latest' | 'gemini-1.5-pro-001' | 'gemini-1.5-pro-002' | 'gemini-2.0-flash' | 'gemini-2.0-flash-001' | 'gemini-2.0-flash-live-001' | 'gemini-2.0-flash-lite' | 'gemini-2.0-pro-exp-02-05' | 'gemini-2.0-flash-thinking-exp-01-21' | 'gemini-2.0-flash-exp' | 'gemini-2.5-pro' | 'gemini-2.5-flash' | 'gemini-2.5-flash-image-preview' | 'gemini-2.5-flash-lite' | 'gemini-2.5-flash-lite-preview-09-2025' | 'gemini-2.5-flash-preview-04-17' | 'gemini-2.5-flash-preview-09-2025' | 'gemini-3-pro-preview' | 'gemini-pro-latest' | 'gemini-flash-latest' | 'gemini-flash-lite-latest' | 'gemini-2.5-pro-exp-03-25' | 'gemini-exp-1206' | 'gemma-3-12b-it' | 'gemma-3-27b-it' | (string & {});
+type GoogleGenerativeAIModelId = 'gemini-2.0-flash' | 'gemini-2.0-flash-001' | 'gemini-2.0-flash-live-001' | 'gemini-2.0-flash-lite' | 'gemini-2.0-pro-exp-02-05' | 'gemini-2.0-flash-thinking-exp-01-21' | 'gemini-2.0-flash-exp' | 'gemini-2.5-pro' | 'gemini-2.5-flash' | 'gemini-2.5-flash-image-preview' | 'gemini-2.5-flash-lite' | 'gemini-2.5-flash-lite-preview-09-2025' | 'gemini-2.5-flash-preview-04-17' | 'gemini-2.5-flash-preview-09-2025' | 'gemini-3-pro-preview' | 'gemini-pro-latest' | 'gemini-flash-latest' | 'gemini-flash-lite-latest' | 'gemini-2.5-pro-exp-03-25' | 'gemini-exp-1206' | 'gemma-3-12b-it' | 'gemma-3-27b-it' | (string & {});
 declare const googleGenerativeAIProviderOptions: _ai_sdk_provider_utils.LazySchema<{
     responseModalities?: ("TEXT" | "IMAGE")[] | undefined;
     thinkingConfig?: {
diff --git a/dist/index.js b/dist/index.js
index c2ef089c42e13a8ee4a833899a415564130e5d79..f8fcddad252106637209660086c731c7f4fd79df 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -587,9 +587,7 @@ function prepareTools({
   var _a;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
   const toolWarnings = [];
-  const isGemini2 = modelId.includes("gemini-2");
-  const supportsDynamicRetrieval = modelId.includes("gemini-1.5-flash") && !modelId.includes("-8b");
-  const supportsFileSearch = modelId.includes("gemini-2.5");
+  const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3") || (modelId.includes("gemini") && modelId.includes("latest"));
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
   }
@@ -613,42 +611,13 @@ function prepareTools({
     providerDefinedTools.forEach((tool) => {
       switch (tool.id) {
         case "google.google_search":
-          if (isGemini2) {
-            googleTools2.push({ googleSearch: {} });
-          } else if (supportsDynamicRetrieval) {
-            googleTools2.push({
-              googleSearchRetrieval: {
-                dynamicRetrievalConfig: {
-                  mode: tool.args.mode,
-                  dynamicThreshold: tool.args.dynamicThreshold
-                }
-              }
-            });
-          } else {
-            googleTools2.push({ googleSearchRetrieval: {} });
-          }
+          googleTools2.push({ googleSearch: {} });
           break;
         case "google.url_context":
-          if (isGemini2) {
-            googleTools2.push({ urlContext: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The URL context tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ urlContext: {} });
           break;
         case "google.code_execution":
-          if (isGemini2) {
-            googleTools2.push({ codeExecution: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The code execution tools is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ codeExecution: {} });
           break;
         case "google.file_search":
           if (supportsFileSearch) {
@@ -662,24 +631,16 @@ function prepareTools({
           }
           break;
         case "google.vertex_rag_store":
-          if (isGemini2) {
-            googleTools2.push({
-              retrieval: {
-                vertex_rag_store: {
-                  rag_resources: {
-                    rag_corpus: tool.args.ragCorpus
-                  },
-                  similarity_top_k: tool.args.topK
-                }
+          googleTools2.push({
+            retrieval: {
+              vertex_rag_store: {
+                rag_resources: {
+                  rag_corpus: tool.args.ragCorpus
+                },
+                similarity_top_k: tool.args.topK
               }
-            });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The RAG store tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+            }
+          });
           break;
         default:
           toolWarnings.push({ type: "unsupported-tool", tool });
diff --git a/dist/index.mjs b/dist/index.mjs
index d75c0cc13c41192408c1f3f2d29d76a7bffa6268..64f1939bcfcadfff7bd78145d6ec2a281b6360a9 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -598,9 +598,7 @@ function prepareTools({
   var _a;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
   const toolWarnings = [];
-  const isGemini2 = modelId.includes("gemini-2");
-  const supportsDynamicRetrieval = modelId.includes("gemini-1.5-flash") && !modelId.includes("-8b");
-  const supportsFileSearch = modelId.includes("gemini-2.5");
+  const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3") || (modelId.includes("gemini") && modelId.includes("latest"));
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
   }
@@ -624,42 +622,13 @@ function prepareTools({
     providerDefinedTools.forEach((tool) => {
       switch (tool.id) {
         case "google.google_search":
-          if (isGemini2) {
-            googleTools2.push({ googleSearch: {} });
-          } else if (supportsDynamicRetrieval) {
-            googleTools2.push({
-              googleSearchRetrieval: {
-                dynamicRetrievalConfig: {
-                  mode: tool.args.mode,
-                  dynamicThreshold: tool.args.dynamicThreshold
-                }
-              }
-            });
-          } else {
-            googleTools2.push({ googleSearchRetrieval: {} });
-          }
+          googleTools2.push({ googleSearch: {} });
           break;
         case "google.url_context":
-          if (isGemini2) {
-            googleTools2.push({ urlContext: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The URL context tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ urlContext: {} });
           break;
         case "google.code_execution":
-          if (isGemini2) {
-            googleTools2.push({ codeExecution: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The code execution tools is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ codeExecution: {} });
           break;
         case "google.file_search":
           if (supportsFileSearch) {
@@ -673,24 +642,16 @@ function prepareTools({
           }
           break;
         case "google.vertex_rag_store":
-          if (isGemini2) {
-            googleTools2.push({
-              retrieval: {
-                vertex_rag_store: {
-                  rag_resources: {
-                    rag_corpus: tool.args.ragCorpus
-                  },
-                  similarity_top_k: tool.args.topK
-                }
+          googleTools2.push({
+            retrieval: {
+              vertex_rag_store: {
+                rag_resources: {
+                  rag_corpus: tool.args.ragCorpus
+                },
+                similarity_top_k: tool.args.topK
               }
-            });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The RAG store tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+            }
+          });
           break;
         default:
           toolWarnings.push({ type: "unsupported-tool", tool });
diff --git a/dist/internal/index.js b/dist/internal/index.js
index 277cac8dc734bea2fb4f3e9a225986b402b24f48..19234c54c5e86d59286de19e022c055c5cbd5931 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -432,9 +432,7 @@ function prepareTools({
   var _a;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
   const toolWarnings = [];
-  const isGemini2 = modelId.includes("gemini-2");
-  const supportsDynamicRetrieval = modelId.includes("gemini-1.5-flash") && !modelId.includes("-8b");
-  const supportsFileSearch = modelId.includes("gemini-2.5");
+  const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3") || (modelId.includes("gemini") && modelId.includes("latest"));
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
   }
@@ -458,42 +456,13 @@ function prepareTools({
     providerDefinedTools.forEach((tool) => {
       switch (tool.id) {
         case "google.google_search":
-          if (isGemini2) {
-            googleTools2.push({ googleSearch: {} });
-          } else if (supportsDynamicRetrieval) {
-            googleTools2.push({
-              googleSearchRetrieval: {
-                dynamicRetrievalConfig: {
-                  mode: tool.args.mode,
-                  dynamicThreshold: tool.args.dynamicThreshold
-                }
-              }
-            });
-          } else {
-            googleTools2.push({ googleSearchRetrieval: {} });
-          }
+          googleTools2.push({ googleSearch: {} });
           break;
         case "google.url_context":
-          if (isGemini2) {
-            googleTools2.push({ urlContext: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The URL context tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ urlContext: {} });
           break;
         case "google.code_execution":
-          if (isGemini2) {
-            googleTools2.push({ codeExecution: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The code execution tools is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ codeExecution: {} });
           break;
         case "google.file_search":
           if (supportsFileSearch) {
@@ -507,24 +476,16 @@ function prepareTools({
           }
           break;
         case "google.vertex_rag_store":
-          if (isGemini2) {
-            googleTools2.push({
-              retrieval: {
-                vertex_rag_store: {
-                  rag_resources: {
-                    rag_corpus: tool.args.ragCorpus
-                  },
-                  similarity_top_k: tool.args.topK
-                }
+          googleTools2.push({
+            retrieval: {
+              vertex_rag_store: {
+                rag_resources: {
+                  rag_corpus: tool.args.ragCorpus
+                },
+                similarity_top_k: tool.args.topK
               }
-            });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The RAG store tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+            }
+          });
           break;
         default:
           toolWarnings.push({ type: "unsupported-tool", tool });
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 03b7cc591be9b58bcc2e775a96740d9f98862a10..186b06d3a91aa9481fce50baf3dafa2a3542258d 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -424,9 +424,7 @@ function prepareTools({
   var _a;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
   const toolWarnings = [];
-  const isGemini2 = modelId.includes("gemini-2");
-  const supportsDynamicRetrieval = modelId.includes("gemini-1.5-flash") && !modelId.includes("-8b");
-  const supportsFileSearch = modelId.includes("gemini-2.5");
+  const supportsFileSearch = modelId.includes("gemini-2.5") || modelId.includes("gemini-3") || (modelId.includes("gemini") && modelId.includes("latest"));
   if (tools == null) {
     return { tools: void 0, toolConfig: void 0, toolWarnings };
   }
@@ -450,42 +448,13 @@ function prepareTools({
     providerDefinedTools.forEach((tool) => {
       switch (tool.id) {
         case "google.google_search":
-          if (isGemini2) {
-            googleTools2.push({ googleSearch: {} });
-          } else if (supportsDynamicRetrieval) {
-            googleTools2.push({
-              googleSearchRetrieval: {
-                dynamicRetrievalConfig: {
-                  mode: tool.args.mode,
-                  dynamicThreshold: tool.args.dynamicThreshold
-                }
-              }
-            });
-          } else {
-            googleTools2.push({ googleSearchRetrieval: {} });
-          }
+          googleTools2.push({ googleSearch: {} });
           break;
         case "google.url_context":
-          if (isGemini2) {
-            googleTools2.push({ urlContext: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The URL context tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ urlContext: {} });
           break;
         case "google.code_execution":
-          if (isGemini2) {
-            googleTools2.push({ codeExecution: {} });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The code execution tools is not supported with other Gemini models than Gemini 2."
-            });
-          }
+          googleTools2.push({ codeExecution: {} });
           break;
         case "google.file_search":
           if (supportsFileSearch) {
@@ -499,24 +468,16 @@ function prepareTools({
           }
           break;
         case "google.vertex_rag_store":
-          if (isGemini2) {
-            googleTools2.push({
-              retrieval: {
-                vertex_rag_store: {
-                  rag_resources: {
-                    rag_corpus: tool.args.ragCorpus
-                  },
-                  similarity_top_k: tool.args.topK
-                }
+          googleTools2.push({
+            retrieval: {
+              vertex_rag_store: {
+                rag_resources: {
+                  rag_corpus: tool.args.ragCorpus
+                },
+                similarity_top_k: tool.args.topK
               }
-            });
-          } else {
-            toolWarnings.push({
-              type: "unsupported-tool",
-              tool,
-              details: "The RAG store tool is not supported with other Gemini models than Gemini 2."
-            });
-          }
+            }
+          });
           break;
         default:
           toolWarnings.push({ type: "unsupported-tool", tool });
